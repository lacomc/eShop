$schema: "1.0"

name: "laco-stanwell-secret-task-old-version"

image: microsoftvisualstudio_visualstudioplustools_vs-2022-ent-general-win11-m365-gen2

description: "Dev Box team customisation file for the TTT project"


tasks:

  # IMPORTANT: Do not remove the six customisation tasks below.

  # They install tools and remove software as required by security.

  # Removing any of these tasks is a breach of Stanwell's security policy.


  # Add your team's tasks to this "tasks" section if they should be run

  # during deployment. Note that these will be run by the computer

  # account.


  # Tasks added to the "userTasks" section will be run as the developer's

  # user account (e.g. flast_b@stanwell.com). These tasks will not run

  # during deployment. They run when the developer first logs in to their

  # Dev Box.


  # Download and install CrowdStrike Falcon Sensor

  - name: powershell

    parameters:

      command: |

        $sensorExeName = 'FalconSensor.exe'

        $sensorPath = Join-Path 'C:/Temp' $sensorExeName  

        az login --service-principal `

          --username beb6c355-7706-40fa-9b37-57ebf7a60012 `

          --password '{{https://dtc-ppe-integrationtests.vault.azure.net/secrets/laco-test-secret/d925c0c61dbb4144ba1068190ff8837a}}' `

          --tenant 556f4662-c7de-4312-9a81-dd4e68521c32 