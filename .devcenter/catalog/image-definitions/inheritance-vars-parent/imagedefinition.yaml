$schema: "1.0"
name: "inheritance-vars-parent"
image: microsoftvisualstudio_visualstudioplustools_vs-2022-ent-general-win11-m365-gen2
description: "This definition is for the eShop frontend engineering environment"

variables:
  message1:
    type: 'string'
    value: 'Hello #1 from inheritance-vars-parent'
    description: "message1 is a variable from the parent definition"
  message2:
    type: 'string'
    value: 'Hello #2 from inheritance-vars-parent'
    description: "message2 is a variable from the parent definition"
  message3:
    type: 'string'
    value: 'Hello #3 from inheritance-vars-parent'
    description: "message3 is a variable from the parent definition"
    
tasks:
  - name: powershell
    parameters:
      command: echo {{message1}}
      
  - name: powershell
    parameters:
      command: echo {{message2}}

userTasks:
  - name: powershell
    parameters:
      command: echo {{message3}}