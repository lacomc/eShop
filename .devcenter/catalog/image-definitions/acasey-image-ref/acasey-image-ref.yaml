$schema: "1.0"
name: "acasey-image-ref"
image: microsoftwindowsdesktop_windows-ent-cpc_win11-24H2-ent-cpc
description: "Copy of mp-hib-enabled using reference image from successful image build"

tasks:
  - name: winget
    parameters:
      downloadUrl: 'https://raw.githubusercontent.com/contoso-co/common-eng-sys/msft-git/dsc-configurations/common-config.dsc.yaml'

  - name: winget
    description: Install AZD
    parameters:
      package:  Microsoft.Azd

  - name: winget
    description: Install Visual Studio Code
    parameters:
      package:  Microsoft.VisualStudioCode

  - name: powershell
    parameters:
      command: |
        $env:Path = [System.Environment]::GetEnvironmentVariable('Path','Machine') + ';' + [System.Environment]::GetEnvironmentVariable('Path','User') 
